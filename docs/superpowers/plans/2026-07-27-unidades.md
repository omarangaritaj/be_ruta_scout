# Unidades: siembra, configuración y separación — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dejar operativa la sección de unidades (siembra automática, configuración inicial y separación) y traducir el módulo completo al inglés en los dos repos y en PowerSync.

**Architecture:** `be_ruta` es dueño de los datos: una colección `units`, el puntero `users.unitId` y una proyección de unión `unit_memberships` que se escriben juntas en una transacción. La siembra vive en un caso de uso único (`UnitSeeder`) que invocan tanto un CLI manual como el respaldo al entrar a la sección. `fe_ruta` consume por `lib/backend/client.ts` y pinta con componentes del banco. PowerSync acota los buckets por `unit_memberships`.

**Tech Stack:** NestJS 11 + Mongoose + Zod + Jest (be_ruta) · Next.js 16 App Router + shadcn base-mira + Vitest (fe_ruta) · MongoDB Atlas · PowerSync self-hosted.

**Spec:** `be_ruta/docs/superpowers/specs/2026-07-27-unidades-design.md`

## Global Constraints

- **Sin comentarios en el código.** Solo se admite un "por qué" no evidente. Nada de comentarios narrativos ni docstrings que expliquen el "qué".
- **Todo el código en inglés**: identificadores, nombres de archivo y estructuras de datos. La prosa (mensajes i18n, textos de commit, documentación) sigue en español.
- **Nunca usar em dashes** en texto de cara al usuario. Guion, dos puntos o paréntesis.
- **Los commits NO se ejecutan sin autorización explícita del usuario.** Cada tarea termina con su paso de commit preparado, pero se ejecuta solo cuando el usuario lo pida. Conventional Commits en español con scope, sin co-authors ni atribución de IA.
- **pnpm siempre**, nunca npm.
- **`components/ui/` no se toca**: es espejo byte a byte de shadcn upstream.
- Verificación de be_ruta: `pnpm verify` (typecheck + lint:check + test + i18n:check + domain:check).
- Verificación de fe_ruta: `pnpm test && pnpm exec tsc --noEmit && pnpm lint && pnpm i18n:check && pnpm domain:check`.
- **El renombrado `unidad` → `unit` va archivo por archivo.** La cadena `unidad` está dentro de `comunidad`, que es una rama del dominio. Un reemplazo global rompe el diccionario.
- Antes de cada commit, actualizar `ROADMAP.md` en el repo que corresponda.

## Estructura de archivos

### `be_ruta`

| Archivo | Responsabilidad |
|---|---|
| `src/units/schemas/unit.schema.ts` | Documento `units` |
| `src/units/schemas/unit-membership.schema.ts` | Documento `unit_memberships` |
| `src/units/seeding/leader-resolution.ts` | Orden de búsqueda del jefe. Puro |
| `src/units/seeding/unit-seeder.ts` | Siembra por grupo. Único algoritmo |
| `src/units/membership-projection.ts` | Deriva filas de membresía desde una unidad. Puro |
| `src/units/unit-scope.ts` | Qué unidades ve cada quien. Puro. Antes `alcance-unidades.ts` |
| `src/units/units.service.ts` | Orquesta las escrituras transaccionales |
| `src/units/units.controller.ts` | HTTP |
| `src/units/units.module.ts` | Cableado Nest |
| `src/units/dto/` | Esquemas Zod |
| `src/tools/seed-units.ts` | CLI de siembra |
| `src/tools/rebuild-unit-memberships.ts` | Reconstruye la proyección |

### `fe_ruta`

| Archivo | Responsabilidad |
|---|---|
| `lib/units/types.ts` | Modelo tipado y helpers puros |
| `lib/powersync/schema.ts` | Espejo local: `units`, `unit_memberships`, `users` |
| `app/(privado)/units/page.tsx` | Listado. SSR |
| `app/(privado)/units/[id]/page.tsx` | Detalle. Server component delgado |
| `app/(privado)/units/[id]/unit-detail-client.tsx` | Lee personas de PowerSync |
| `app/(privado)/units/[id]/configure-dialog.tsx` | Modal de primera vez |
| `app/(privado)/units/[id]/members-form.tsx` | Selección de miembros |
| `app/(privado)/units/[id]/unit-detail-skeleton.tsx` | Estado de carga del sync |
| `app/(privado)/units/actions.ts` | Server actions. Escritura online |
| `components/collection/checkbox-list.tsx` | Lista con checkbox y búsqueda |
| `components/playground/checkbox-list-demo.tsx` | Demo del banco |

### Origen de los datos

El listado es **SSR**: solo el servidor resuelve el alcance de un super admin o de un nivel región, que ven unidades donde no tienen membresía. El detalle lee sus listas de personas de **PowerSync** y escribe siempre contra el **backend**, porque guardar miembros es una transacción sobre tres colecciones, no una escritura encolable.

---

## Fase 0: infraestructura

### Task 1: Replica set — VERIFICADA, NO APLICA

**Comprobado el 2026-07-27: el `.env` real de `be_ruta` apunta a MongoDB Atlas**, no al `mongod` local. Atlas siempre es replica set, así que las transacciones funcionan sin ningún cambio de infraestructura.

El `mongod` local **sí** es standalone (`NoReplicationEnabled`), pero el proyecto no lo usa. El `.env.example` sugería lo contrario y por eso esta tarea existía.

Lo único que queda de la tarea es documental, y se pliega dentro de la Task 10:

- [ ] **Step 1: Dejar constancia del requisito en el README**

En `be_ruta/README.md`, sección de requisitos previos:

```markdown
### MongoDB: replica set obligatorio

El módulo de unidades escribe en tres colecciones dentro de una transacción, y
las transacciones de MongoDB solo existen en un replica set. Atlas lo es siempre.
Si algún día se apunta `MONGODB_URI` a un mongod local, hay que arrancarlo con
`--replSet rs0` y ejecutar `rs.initiate()` una vez, o las escrituras fallarán con
"Transaction numbers are only allowed on a replica set member or mongos".
```

**El resto de esta tarea se elimina.** Contenido histórico, no ejecutar:

**Files:**
- Modify: `be_ruta/README.md`
- Modify: `be_ruta/.env.example`

- [ ] **Step 1: Verificar si el Mongo local ya es replica set**

```bash
mongosh "mongodb://localhost:27017" --quiet --eval "try { rs.status().set } catch(e) { print('standalone') }"
```

Si imprime un nombre de conjunto, la tarea está hecha: salta al Step 4. Si imprime `standalone`, sigue.

- [ ] **Step 2: Arrancar mongod como replica set de un nodo**

Detén el `mongod` actual y arráncalo con:

```bash
mongod --replSet rs0 --dbpath /var/lib/mongodb --bind_ip 127.0.0.1
```

- [ ] **Step 3: Iniciar el conjunto (una sola vez)**

```bash
mongosh "mongodb://localhost:27017" --quiet --eval "rs.initiate({_id:'rs0', members:[{_id:0, host:'127.0.0.1:27017'}]})"
```

Esperado: `{ ok: 1 }`. Verifica con `rs.status().set` que devuelve `rs0`.

- [ ] **Step 4: Documentar el requisito**

En `be_ruta/README.md`, sección de requisitos previos, añade:

```markdown
### MongoDB en desarrollo: replica set obligatorio

El módulo de unidades escribe en tres colecciones dentro de una transacción, y las
transacciones de MongoDB solo existen en un replica set. Atlas ya lo es. Para
desarrollo local, arranca mongod con `--replSet rs0` y ejecuta una vez
`rs.initiate()`. Con un mongod standalone las escrituras fallan con
"Transaction numbers are only allowed on a replica set member or mongos".
```

En `.env.example`, actualiza la línea de ejemplo local a:

```
MONGODB_URI=mongodb://localhost:27017/ruta?replicaSet=rs0
```

- [ ] **Step 5: Commit (solo con autorización del usuario)**

```bash
git add README.md .env.example
git commit -m "docs(be): documentar el requisito de replica set para transacciones"
```

---

## Fase 1: diccionario de dominio

### Task 2: Permisos `unit:*` y vocabulario `unitRoles` en el manifiesto

El manifiesto es idéntico byte a byte en los dos repos y `pnpm domain:check` lo verifica por SHA-256. Se editan los dos y se regeneran los dos.

**Files:**
- Modify: `be_ruta/domain-manifest.json:80-84`
- Modify: `fe_ruta/domain-manifest.json` (mismas líneas)
- Regenerate: `be_ruta/src/domain/`, `fe_ruta/lib/domain/`

**Interfaces:**
- Produces: las claves `'unit:read'`, `'unit:create'`, `'unit:update'`, `'unit:delete'` dentro del tipo `PermissionKey`. `UNIT_ROLES` = `['unit_leader', 'assistant', 'member']`, tipo `UnitRole`, accesor `D.UNIT_ROLE.UNIT_LEADER | ASSISTANT | MEMBER`.
- **No existe `D.PERMISSION`.** Verificado el 2026-07-27: los permisos se escriben como strings literales en los decoradores (`@RequirePermissions('grupo:create')`) y es el tipo `PermissionKey` quien los restringe. Los vocabularios de valores (ramas, roles, tipos de persona) sí tienen accesor en `D`; los permisos no. No inventes uno.

- [ ] **Step 1: Cambiar los cuatro permisos en el manifiesto de be_ruta**

En `be_ruta/domain-manifest.json`, sustituye el bloque de unidades por:

```json
    { "key": "unit:read", "side": "both" },
    { "key": "unit:create", "side": "both" },
    { "key": "unit:update", "side": "both" },
    { "key": "unit:delete", "side": "both" },
```

- [ ] **Step 2: Añadir el vocabulario `unitRoles`**

En el mismo archivo, después del bloque `personTypes`, añade:

```json
  "unitRoles": [
    { "name": "UNIT_LEADER", "value": "unit_leader", "order": 1 },
    { "name": "ASSISTANT", "value": "assistant", "order": 2 },
    { "name": "MEMBER", "value": "member", "order": 3 }
  ],
```

- [ ] **Step 3: Enseñar al generador a emitir `unitRoles`**

Abre `be_ruta/scripts/domain-codegen.ts` y localiza cómo se emite `personTypes`. Replica el mismo tratamiento para `unitRoles`, produciendo `UNIT_ROLES`, el tipo `UnitRole` y la entrada `UNIT_ROLE` del accesor `D`. Si el generador ya itera una lista de vocabularios declarada en un solo sitio, basta con añadir `unitRoles` a esa lista.

- [ ] **Step 4: Ejecutar el test del generador**

Run: `cd be_ruta && pnpm exec jest scripts/domain-codegen.spec.ts`
Expected: PASS. Si falla porque el snapshot no contempla `unitRoles`, actualiza el caso de prueba con la salida esperada, nunca borrándolo.

- [ ] **Step 5: Copiar el manifiesto al frontend, byte a byte**

```bash
cp be_ruta/domain-manifest.json fe_ruta/domain-manifest.json
sha256sum be_ruta/domain-manifest.json fe_ruta/domain-manifest.json
```

Expected: los dos hashes idénticos.

- [ ] **Step 6: Regenerar el dominio en los dos repos**

```bash
cd be_ruta && pnpm domain:gen && pnpm domain:check
cd ../fe_ruta && pnpm domain:gen && pnpm domain:check
```

Expected: ambos `domain:check` en verde.

- [ ] **Step 7: Verificar que el vocabulario nuevo existe**

```bash
cd be_ruta && rg -n "UNIT_ROLES|UnitRole|unit:read" src/domain/
cd ../fe_ruta && rg -n "UNIT_ROLES|UnitRole|unit:read" lib/domain/
```

Expected: hits en los dos.

- [ ] **Step 8: Commit (solo con autorización del usuario)**

```bash
git add domain-manifest.json src/domain scripts/domain-codegen.ts
git commit -m "feat(dominio): renombrar permisos de unidad a unit y agregar unitRoles"
```

---

## Fase 2: backend

### Task 3: Esquemas `Unit` y `UnitMembership`

**Files:**
- Create: `be_ruta/src/units/schemas/unit.schema.ts`
- Create: `be_ruta/src/units/schemas/unit-membership.schema.ts`
- Delete: `be_ruta/src/unidades/schemas/unidad.schema.ts`

**Interfaces:**
- Produces: `Unit`, `UnitSchema`, `UnitDocument`; `UnitMembership`, `UnitMembershipSchema`, `UnitMembershipDocument`.

- [ ] **Step 1: Crear el esquema de unidad**

```ts
import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';
import { HydratedDocument, Schema as MongooseSchema, Types } from 'mongoose';
import { BRANCHES, type Branch } from '../../domain';

export type UnitDocument = HydratedDocument<Unit>;

@Schema({ collection: 'units', timestamps: true })
export class Unit {
  @Prop({ required: true, trim: true })
  name: string;

  @Prop({ type: String, enum: BRANCHES, required: true, index: true })
  branch: Branch;

  @Prop({ type: Number, required: true, index: true })
  groupId: number;

  @Prop({ type: Number, index: true })
  districtId?: number;

  @Prop({ trim: true })
  districtName?: string;

  @Prop({ trim: true })
  city?: string;

  @Prop({ type: MongooseSchema.Types.ObjectId, ref: 'User', required: true })
  unitLeaderId: Types.ObjectId;

  @Prop({ type: [MongooseSchema.Types.ObjectId], ref: 'User', default: [] })
  leaders: Types.ObjectId[];

  @Prop({ type: [MongooseSchema.Types.ObjectId], ref: 'User', default: [] })
  members: Types.ObjectId[];

  @Prop({ type: Date })
  configuredAt?: Date;
}

export const UnitSchema = SchemaFactory.createForClass(Unit);

UnitSchema.index({ groupId: 1, name: 1 }, { unique: true });
```

- [ ] **Step 2: Crear el esquema de membresía**

```ts
import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';
import { HydratedDocument, Schema as MongooseSchema, Types } from 'mongoose';
import { UNIT_ROLES, type UnitRole } from '../../domain';

export type UnitMembershipDocument = HydratedDocument<UnitMembership>;

@Schema({ collection: 'unit_memberships', timestamps: true })
export class UnitMembership {
  @Prop({ type: MongooseSchema.Types.ObjectId, ref: 'User', required: true })
  userId: Types.ObjectId;

  @Prop({ type: MongooseSchema.Types.ObjectId, ref: 'Unit', required: true })
  unitId: Types.ObjectId;

  @Prop({ type: String, enum: UNIT_ROLES, required: true })
  role: UnitRole;

  @Prop({ type: Number, required: true, index: true })
  groupId: number;
}

export const UnitMembershipSchema =
  SchemaFactory.createForClass(UnitMembership);

UnitMembershipSchema.index({ userId: 1, unitId: 1 }, { unique: true });
UnitMembershipSchema.index({ unitId: 1 });
```

- [ ] **Step 3: Borrar el esquema viejo**

```bash
rm be_ruta/src/unidades/schemas/unidad.schema.ts
```

El proyecto no compilará hasta la Task 8, que recablea el módulo. Es esperado.

- [ ] **Step 4: Commit (solo con autorización del usuario)**

```bash
git add src/units/schemas src/unidades/schemas
git commit -m "feat(units): agregar esquemas Unit y UnitMembership en ingles"
```

---

### Task 4: Proyección de membresías (función pura)

Deriva las filas de `unit_memberships` a partir de una unidad. Al ser pura se testea sin base de datos y es la misma función que usan el seeder, el service y el reconstructor, así que las tres rutas no pueden divergir.

**Files:**
- Create: `be_ruta/src/units/membership-projection.ts`
- Test: `be_ruta/src/units/membership-projection.spec.ts`

**Interfaces:**
- Produces: `projectMemberships(unit: ProjectableUnit): MembershipRow[]`, con `ProjectableUnit = { _id, groupId, unitLeaderId, leaders, members }` y `MembershipRow = { userId, unitId, role, groupId }`. Todos los ids como `string`.

- [ ] **Step 1: Escribir el test que falla**

```ts
import { projectMemberships } from './membership-projection';

const unit = {
  _id: 'u1',
  groupId: 304,
  unitLeaderId: 'a1',
  leaders: ['a2', 'a3'],
  members: ['p1', 'p2'],
};

describe('projectMemberships', () => {
  it('produce una fila por persona con su rol', () => {
    expect(projectMemberships(unit)).toEqual([
      { userId: 'a1', unitId: 'u1', role: 'unit_leader', groupId: 304 },
      { userId: 'a2', unitId: 'u1', role: 'assistant', groupId: 304 },
      { userId: 'a3', unitId: 'u1', role: 'assistant', groupId: 304 },
      { userId: 'p1', unitId: 'u1', role: 'member', groupId: 304 },
      { userId: 'p2', unitId: 'u1', role: 'member', groupId: 304 },
    ]);
  });

  it('el jefe gana si tambien aparece como subjefe', () => {
    const rows = projectMemberships({ ...unit, leaders: ['a1', 'a2'] });
    expect(rows.filter((r) => r.userId === 'a1')).toEqual([
      { userId: 'a1', unitId: 'u1', role: 'unit_leader', groupId: 304 },
    ]);
  });

  it('una unidad sin subjefes solo proyecta jefe y miembros', () => {
    expect(projectMemberships({ ...unit, leaders: [] })).toHaveLength(3);
  });
});
```

- [ ] **Step 2: Ejecutar el test para verificar que falla**

Run: `cd be_ruta && pnpm exec jest src/units/membership-projection.spec.ts`
Expected: FAIL, "Cannot find module './membership-projection'".

- [ ] **Step 3: Implementar la proyección**

```ts
import { D, type UnitRole } from '../domain';

export interface ProjectableUnit {
  _id: string;
  groupId: number;
  unitLeaderId: string;
  leaders: string[];
  members: string[];
}

export interface MembershipRow {
  userId: string;
  unitId: string;
  role: UnitRole;
  groupId: number;
}

export function projectMemberships(unit: ProjectableUnit): MembershipRow[] {
  const seen = new Set<string>();
  const rows: MembershipRow[] = [];

  const add = (userId: string, role: UnitRole) => {
    if (seen.has(userId)) return;
    seen.add(userId);
    rows.push({ userId, unitId: unit._id, role, groupId: unit.groupId });
  };

  add(unit.unitLeaderId, D.UNIT_ROLE.UNIT_LEADER);
  unit.leaders.forEach((id) => add(id, D.UNIT_ROLE.ASSISTANT));
  unit.members.forEach((id) => add(id, D.UNIT_ROLE.MEMBER));

  return rows;
}
```

- [ ] **Step 4: Ejecutar el test para verificar que pasa**

Run: `cd be_ruta && pnpm exec jest src/units/membership-projection.spec.ts`
Expected: PASS, 3 tests.

- [ ] **Step 5: Commit (solo con autorización del usuario)**

```bash
git add src/units/membership-projection.ts src/units/membership-projection.spec.ts
git commit -m "feat(units): proyectar membresias de unidad como funcion pura"
```

---

### Task 5: Resolución del jefe de unidad (función pura)

**Files:**
- Create: `be_ruta/src/units/seeding/leader-resolution.ts`
- Test: `be_ruta/src/units/seeding/leader-resolution.spec.ts`

**Interfaces:**
- Consumes: `ramaDeCargo` de `../../catalogo-cargos/catalogo-cargos`, `D` de `../../domain`.
- Produces: `resolveUnitLeader(branch: Branch, adults: LeaderCandidate[]): LeaderCandidate | undefined`, con `LeaderCandidate = { _id: string; name: string; cargoSiscout?: string; cargos?: { nombreCargo: string; nivel: string }[] }`.

- [ ] **Step 1: Escribir el test que falla**

```ts
import { resolveUnitLeader } from './leader-resolution';

const branchChief = {
  _id: 'a1',
  name: 'Zulema Ruiz',
  cargoSiscout: 'JEFE DE MANADA',
};
const groupChief = { _id: 'a2', name: 'Bruno Diaz', cargoSiscout: 'JEFE DE GRUPO' };
const collaborator = {
  _id: 'a3',
  name: 'Carla Pena',
  cargoSiscout: 'COLABORADOR DE GRUPO',
};
const anyAdult = { _id: 'a4', name: 'Alba Soto', cargoSiscout: 'ACOMPANANTE' };

describe('resolveUnitLeader', () => {
  it('el jefe de rama gana a todos', () => {
    const all = [anyAdult, collaborator, groupChief, branchChief];
    expect(resolveUnitLeader('manada', all)?._id).toBe('a1');
  });

  it('sin jefe de rama pasa al jefe de grupo', () => {
    expect(
      resolveUnitLeader('manada', [anyAdult, collaborator, groupChief])?._id,
    ).toBe('a2');
  });

  it('sin jefe de grupo pasa al colaborador', () => {
    expect(resolveUnitLeader('manada', [anyAdult, collaborator])?._id).toBe('a3');
  });

  it('en ultimo lugar cualquier adulto', () => {
    expect(resolveUnitLeader('manada', [anyAdult])?._id).toBe('a4');
  });

  it('el jefe de otra rama no sirve para esta', () => {
    const otherBranch = {
      _id: 'a5',
      name: 'Elsa Mora',
      cargoSiscout: 'JEFE DE TROPA',
    };
    expect(resolveUnitLeader('manada', [otherBranch, groupChief])?._id).toBe('a2');
  });

  it('el cargo asignado por la plataforma gana al de SiScout', () => {
    const assigned = {
      _id: 'a6',
      name: 'Nora Gil',
      cargoSiscout: 'ACOMPANANTE',
      cargos: [{ nombreCargo: 'JEFE DE MANADA', nivel: 'rama' }],
    };
    expect(resolveUnitLeader('manada', [groupChief, assigned])?._id).toBe('a6');
  });

  it('el titular gana al subjefe dentro de la misma rama', () => {
    const deputy = {
      _id: 'a7',
      name: 'Aaron Paz',
      cargoSiscout: 'SUB-JEFE DE MANADA',
    };
    expect(resolveUnitLeader('manada', [deputy, branchChief])?._id).toBe('a1');
  });

  it('desempata alfabeticamente para ser determinista', () => {
    const first = { _id: 'a8', name: 'Ana Lopez', cargoSiscout: 'ACOMPANANTE' };
    const second = { _id: 'a9', name: 'Zoe Marin', cargoSiscout: 'ACOMPANANTE' };
    expect(resolveUnitLeader('manada', [second, first])?._id).toBe('a8');
  });

  it('sin adultos no hay jefe', () => {
    expect(resolveUnitLeader('manada', [])).toBeUndefined();
  });
});
```

- [ ] **Step 2: Ejecutar el test para verificar que falla**

Run: `cd be_ruta && pnpm exec jest src/units/seeding/leader-resolution.spec.ts`
Expected: FAIL, "Cannot find module './leader-resolution'".

- [ ] **Step 3: Implementar la resolución**

```ts
import { ramaDeCargo } from '../../catalogo-cargos/catalogo-cargos';
import { D, type Branch } from '../../domain';

export interface LeaderCandidate {
  _id: string;
  name: string;
  cargoSiscout?: string;
  cargos?: { nombreCargo: string; nivel: string }[];
}

const GROUP_CHIEF_TITLES = ['JEFE DE GRUPO', 'SUBJEFE DE GRUPO'];
const COLLABORATOR = 'COLABORADOR';

const DIACRITICS = /\p{Diacritic}/gu;

const normalize = (value: string): string =>
  value
    .normalize('NFD')
    .replace(DIACRITICS, '')
    .toUpperCase()
    .replace(/\s+/g, ' ')
    .trim();

function titlesOf(candidate: LeaderCandidate): string[] {
  const assigned = (candidate.cargos ?? [])
    .filter((c) => c.nivel === D.ROLE_LEVEL.RAMA)
    .map((c) => c.nombreCargo);
  return candidate.cargoSiscout
    ? [...assigned, candidate.cargoSiscout]
    : assigned;
}

function leadsBranch(candidate: LeaderCandidate, branch: Branch): boolean {
  return titlesOf(candidate).some((title) => ramaDeCargo(title) === branch);
}

function isDeputy(candidate: LeaderCandidate, branch: Branch): boolean {
  return titlesOf(candidate).some(
    (title) => ramaDeCargo(title) === branch && normalize(title).startsWith('SUB'),
  );
}

function byName(a: LeaderCandidate, b: LeaderCandidate): number {
  return a.name.localeCompare(b.name);
}

export function resolveUnitLeader(
  branch: Branch,
  adults: LeaderCandidate[],
): LeaderCandidate | undefined {
  const sorted = [...adults].sort(byName);

  const branchLeaders = sorted.filter((a) => leadsBranch(a, branch));
  const titular = branchLeaders.find((a) => !isDeputy(a, branch));
  if (titular) return titular;
  if (branchLeaders.length > 0) return branchLeaders[0];

  for (const title of GROUP_CHIEF_TITLES) {
    const found = sorted.find(
      (a) => a.cargoSiscout && normalize(a.cargoSiscout) === title,
    );
    if (found) return found;
  }

  const collaborator = sorted.find(
    (a) => a.cargoSiscout && normalize(a.cargoSiscout).includes(COLLABORATOR),
  );
  if (collaborator) return collaborator;

  return sorted[0];
}
```

- [ ] **Step 4: Ejecutar el test para verificar que pasa**

Run: `cd be_ruta && pnpm exec jest src/units/seeding/leader-resolution.spec.ts`
Expected: PASS, 9 tests.

- [ ] **Step 5: Commit (solo con autorización del usuario)**

```bash
git add src/units/seeding/leader-resolution.ts src/units/seeding/leader-resolution.spec.ts
git commit -m "feat(units): resolver el jefe de unidad por orden de cargo"
```

---

### Task 6: Alcance de unidades traducido

Renombra `alcance-unidades.ts` a `unit-scope.ts` con todo en inglés. La lógica no cambia: solo los nombres y los discriminantes.

**Files:**
- Create: `be_ruta/src/units/unit-scope.ts`
- Test: `be_ruta/src/units/unit-scope.spec.ts`
- Delete: `be_ruta/src/unidades/alcance-unidades.ts`, `be_ruta/src/unidades/alcance-unidades.spec.ts`

**Interfaces:**
- Produces: `resolveUnitScope(profile: ScopeProfile): UnitScope`, con `UnitScope = { type: 'all' } | { type: 'group'; groupId } | { type: 'branch'; branch; groupId } | { type: 'leadership-required'; groupId } | { type: 'no-group' }`.

- [ ] **Step 1: Portar el test existente con los nombres nuevos**

Copia `src/unidades/alcance-unidades.spec.ts` a `src/units/unit-scope.spec.ts` y aplica estos reemplazos, uno a uno:

| Antes | Ahora |
|---|---|
| `resolverAlcance` | `resolveUnitScope` |
| `'./alcance-unidades'` | `'./unit-scope'` |
| `type: 'grupo'` | `type: 'group'` |
| `type: 'rama'` | `type: 'branch'` |
| `type: 'jefatura-requerida'` | `type: 'leadership-required'` |
| `type: 'sin-grupo'` | `type: 'no-group'` |
| `rama: 'tropa'` | `branch: 'tropa'` |

Los **valores** de rama (`'tropa'`, `'manada'`) NO se traducen: son vocabulario del dominio.

- [ ] **Step 2: Ejecutar el test para verificar que falla**

Run: `cd be_ruta && pnpm exec jest src/units/unit-scope.spec.ts`
Expected: FAIL, "Cannot find module './unit-scope'".

- [ ] **Step 3: Portar la implementación**

Copia `src/unidades/alcance-unidades.ts` a `src/units/unit-scope.ts` y traduce: `AlcanceUnidades` a `UnitScope`, `PerfilParaAlcance` a `ScopeProfile`, `resolverAlcance` a `resolveUnitScope`, `ramaAsignada` a `assignedBranch`, `ramaSegunSiscout` a `siscoutBranch`, `NIVELES_SIN_FILTRO` a `UNFILTERED_LEVELS`, `CARGOS_DE_TODO_EL_GRUPO` a `WHOLE_GROUP_TITLES`, y los campos de la unión según la tabla del Step 1.

Ajusta los imports a `../domain`, `../catalogo-cargos/...` (un nivel, no dos).

Revisa si el `eslint-disable no-restricted-syntax` sigue haciendo falta: los discriminantes en inglés (`'group'`, `'branch'`) ya no colisionan con el vocabulario del manifiesto, que está en español. Si `pnpm lint:check` pasa sin él, quítalo junto con su comentario.

- [ ] **Step 4: Ejecutar el test para verificar que pasa**

Run: `cd be_ruta && pnpm exec jest src/units/unit-scope.spec.ts`
Expected: PASS, mismo número de tests que el original.

- [ ] **Step 5: Borrar los archivos viejos**

```bash
rm be_ruta/src/unidades/alcance-unidades.ts be_ruta/src/unidades/alcance-unidades.spec.ts
```

- [ ] **Step 6: Commit (solo con autorización del usuario)**

```bash
git add src/units/unit-scope.ts src/units/unit-scope.spec.ts src/unidades
git commit -m "refactor(units): traducir el alcance de unidades a ingles"
```

---

### Task 7: Sembrador de unidades

**Files:**
- Create: `be_ruta/src/units/seeding/unit-seeder.ts`
- Test: `be_ruta/src/units/seeding/unit-seeder.spec.ts`

**Interfaces:**
- Consumes: `resolveUnitLeader` (Task 5), `projectMemberships` (Task 4), `ramaDeEtiquetaSiscout` de `../../catalogo-cargos/ramas`, `BRANCH_MESSAGE_KEY` y `D` de `../../domain`.
- Produces: `planGroupSeed(input: SeedInput): SeedPlan`, puro. `SeedInput = { groupId: number; people: SeedPerson[] }`, `SeedPerson = { _id, name, tipo, cargoSiscout?, cargos?, districtId?, districtName? }`. `SeedPlan = { units: PlannedUnit[]; skipped?: SeedSkipReason }`, `PlannedUnit = { name, branch, groupId, districtId?, districtName?, unitLeaderId, leaders: [], members: string[] }`, `SeedSkipReason = 'no-people' | 'no-adults' | 'no-protagonists'`.

La clase `UnitSeeder`, que hace la escritura en Mongo, se añade en la Task 8. Esta tarea deja la **decisión** aislada y pura para poder testearla sin base de datos.

- [ ] **Step 1: Escribir el test que falla**

```ts
import { planGroupSeed } from './unit-seeder';

const chief = {
  _id: 'a1',
  name: 'Zulema Ruiz',
  tipo: 'adulto' as const,
  cargoSiscout: 'JEFE DE MANADA',
  districtId: 12,
  districtName: 'Distrito Norte',
};
const cub = { _id: 'p1', name: 'Ana Ruiz', tipo: 'protagonista' as const, cargoSiscout: 'LOBATO' };
const cub2 = { _id: 'p2', name: 'Beto Paz', tipo: 'protagonista' as const, cargoSiscout: 'LOBATOS' };
const scout = { _id: 'p3', name: 'Cesar Mora', tipo: 'protagonista' as const, cargoSiscout: 'SCOUT' };

describe('planGroupSeed', () => {
  it('crea una unidad por rama con protagonistas', () => {
    const plan = planGroupSeed({ groupId: 304, people: [chief, cub, cub2, scout] });
    expect(plan.units).toHaveLength(2);
    expect(plan.units.map((u) => u.branch).sort()).toEqual(['manada', 'tropa']);
  });

  it('agrupa a los protagonistas de la misma rama pese a los alias', () => {
    const plan = planGroupSeed({ groupId: 304, people: [chief, cub, cub2] });
    expect(plan.units[0].members.sort()).toEqual(['p1', 'p2']);
  });

  it('nombra la unidad con el texto de cambio pendiente', () => {
    const plan = planGroupSeed({ groupId: 304, people: [chief, cub] });
    expect(plan.units[0].name).toBe('cambiar nombre unidad manada');
  });

  it('hereda el distrito del primer usuario que lo tenga', () => {
    const plan = planGroupSeed({ groupId: 304, people: [chief, cub] });
    expect(plan.units[0].districtId).toBe(12);
    expect(plan.units[0].districtName).toBe('Distrito Norte');
  });

  it('asigna el jefe de rama a su unidad', () => {
    const plan = planGroupSeed({ groupId: 304, people: [chief, cub, scout] });
    const pack = plan.units.find((u) => u.branch === 'manada');
    expect(pack?.unitLeaderId).toBe('a1');
  });

  it('cae al jefe de grupo en la rama que no tiene jefatura propia', () => {
    const plan = planGroupSeed({ groupId: 304, people: [chief, cub, scout] });
    const troop = plan.units.find((u) => u.branch === 'tropa');
    expect(troop?.unitLeaderId).toBe('a1');
  });

  it('nace sin subjefes', () => {
    const plan = planGroupSeed({ groupId: 304, people: [chief, cub] });
    expect(plan.units[0].leaders).toEqual([]);
  });

  it('sin personas no siembra', () => {
    expect(planGroupSeed({ groupId: 304, people: [] })).toEqual({
      units: [],
      skipped: 'no-people',
    });
  });

  it('sin adultos no puede haber jefe y no siembra', () => {
    expect(planGroupSeed({ groupId: 304, people: [cub] })).toEqual({
      units: [],
      skipped: 'no-adults',
    });
  });

  it('sin protagonistas no hay unidad que crear', () => {
    expect(planGroupSeed({ groupId: 304, people: [chief] })).toEqual({
      units: [],
      skipped: 'no-protagonists',
    });
  });
});
```

- [ ] **Step 2: Ejecutar el test para verificar que falla**

Run: `cd be_ruta && pnpm exec jest src/units/seeding/unit-seeder.spec.ts`
Expected: FAIL, "Cannot find module './unit-seeder'".

- [ ] **Step 3: Implementar la planificación**

```ts
import { ramaDeEtiquetaSiscout } from '../../catalogo-cargos/ramas';
import { BRANCHES, BRANCH_MESSAGE_KEY, D, type Branch } from '../../domain';
import { t } from '../../i18n';
import { resolveUnitLeader, type LeaderCandidate } from './leader-resolution';

export interface SeedPerson extends LeaderCandidate {
  tipo: string;
  districtId?: number;
  districtName?: string;
}

export interface SeedInput {
  groupId: number;
  people: SeedPerson[];
}

export interface PlannedUnit {
  name: string;
  branch: Branch;
  groupId: number;
  districtId?: number;
  districtName?: string;
  unitLeaderId: string;
  leaders: string[];
  members: string[];
}

export type SeedSkipReason = 'no-people' | 'no-adults' | 'no-protagonists';

export interface SeedPlan {
  units: PlannedUnit[];
  skipped?: SeedSkipReason;
}

export function placeholderName(branch: Branch, index = 1): string {
  const base = `cambiar nombre unidad ${branch}`;
  return index > 1 ? `${base} ${index}` : base;
}

function membersByBranch(people: SeedPerson[]): Map<Branch, string[]> {
  const grouped = new Map<Branch, string[]>();

  for (const person of people) {
    if (person.tipo !== D.PERSON_TYPE.PROTAGONIST) continue;
    const branch = ramaDeEtiquetaSiscout(person.cargoSiscout);
    if (!branch) continue;
    grouped.set(branch, [...(grouped.get(branch) ?? []), person._id]);
  }

  return grouped;
}

export function planGroupSeed({ groupId, people }: SeedInput): SeedPlan {
  if (people.length === 0) return { units: [], skipped: 'no-people' };

  const adults = people.filter((p) => p.tipo === D.PERSON_TYPE.ADULT);
  if (adults.length === 0) return { units: [], skipped: 'no-adults' };

  const grouped = membersByBranch(people);
  if (grouped.size === 0) return { units: [], skipped: 'no-protagonists' };

  const withDistrict = people.find((p) => p.districtId !== undefined);

  const units = BRANCHES.filter((branch) => grouped.has(branch)).map(
    (branch): PlannedUnit => ({
      name: placeholderName(branch),
      branch,
      groupId,
      districtId: withDistrict?.districtId,
      districtName: withDistrict?.districtName,
      unitLeaderId: resolveUnitLeader(branch, adults)!._id,
      leaders: [],
      members: grouped.get(branch)!,
    }),
  );

  return { units };
}
```

Nota sobre el `!` de `resolveUnitLeader`: es seguro porque el guard de `adults.length === 0` ya descartó el único caso en que devuelve `undefined`. Si `BRANCH_MESSAGE_KEY` o `t` quedan sin usar tras escribir esto, quita esos imports.

- [ ] **Step 4: Ejecutar el test para verificar que pasa**

Run: `cd be_ruta && pnpm exec jest src/units/seeding/unit-seeder.spec.ts`
Expected: PASS, 10 tests.

- [ ] **Step 5: Commit (solo con autorización del usuario)**

```bash
git add src/units/seeding/unit-seeder.ts src/units/seeding/unit-seeder.spec.ts
git commit -m "feat(units): planificar la siembra de unidades por grupo"
```

---

### Task 8: Servicio, DTOs, controlador y módulo

**Files:**
- Create: `be_ruta/src/units/units.service.ts`, `units.controller.ts`, `units.module.ts`
- Create: `be_ruta/src/units/dto/unit-base.schema.ts`, `configure-unit.dto.ts`, `update-unit.dto.ts`, `set-members.dto.ts`, `declare-leadership.dto.ts`
- Modify: `be_ruta/src/app.module.ts`
- Modify: `be_ruta/src/i18n/catalog.ts:151-156`
- Delete: `be_ruta/src/unidades/` completo

**Interfaces:**
- Consumes: `planGroupSeed`, `placeholderName` (Task 7), `projectMemberships` (Task 4), `resolveUnitScope` (Task 6), `Unit`/`UnitMembership` (Task 3).
- Produces: `UnitsService` con `findAll(user)`, `findOne(id)`, `configure(id, dto)`, `setMembers(id, memberIds)`, `update(id, dto)`, `remove(id)`, `declareLeadership(user, title)`, `seedGroup(groupId)`.

- [ ] **Step 1: Añadir las claves i18n nuevas**

En `be_ruta/src/i18n/catalog.ts`, sustituye el bloque `UNITS` por:

```ts
  UNITS: {
    LEADERSHIP_NOT_A_BRANCH: 'El cargo "{cargo}" no es una jefatura de rama',
    LEADERSHIP_REQUIRED:
      'Tu cargo no determina una rama: indica qué jefatura tienes',
    MEMBERS_NOT_IN_UNIT: 'Algún protagonista no pertenece a esta unidad',
    MEMBERS_REQUIRED: 'Una unidad necesita al menos un protagonista',
    MISSING_GROUP: 'No perteneces a ningún grupo',
    NAME_TAKEN: 'Ya existe una unidad con ese nombre en el grupo',
    NOT_FOUND: 'No existe una unidad con id "{id}"',
  },
```

- [ ] **Step 2: Escribir los DTOs**

`dto/unit-base.schema.ts`:

```ts
import { z } from 'zod';
import { objectIdSchema } from '../../common';
import { BRANCHES } from '../../domain';
import { K, t } from '../../i18n';

export const unitBaseSchema = z.object({
  name: z
    .string({ error: t(K.VALIDATION.REQUIRED_MASCULINE) })
    .trim()
    .min(1, { error: t(K.VALIDATION.NOT_EMPTY_MASCULINE) }),

  branch: z.enum(BRANCHES, {
    error: `debe ser una de: ${BRANCHES.join(', ')}`,
  }),

  groupId: z.number().int().positive(),

  districtId: z.number().int().positive(),

  districtName: z.string().trim(),

  city: z.string().trim(),

  unitLeaderId: objectIdSchema,

  leaders: z.array(objectIdSchema),

  members: z.array(objectIdSchema),
});
```

`dto/configure-unit.dto.ts`:

```ts
import { z } from 'zod';
import { unitBaseSchema } from './unit-base.schema';

export const configureUnitSchema = unitBaseSchema.pick({
  name: true,
  unitLeaderId: true,
  leaders: true,
  city: true,
});

export type ConfigureUnitDto = z.infer<typeof configureUnitSchema>;
```

`dto/update-unit.dto.ts`:

```ts
import { z } from 'zod';
import { K, t } from '../../i18n';
import { unitBaseSchema } from './unit-base.schema';

export const updateUnitSchema = unitBaseSchema
  .partial()
  .refine((data) => Object.keys(data).length > 0, {
    error: t(K.VALIDATION.AT_LEAST_ONE_FIELD),
  });

export type UpdateUnitDto = z.infer<typeof updateUnitSchema>;
```

`dto/set-members.dto.ts`:

```ts
import { z } from 'zod';
import { objectIdSchema } from '../../common';
import { K, t } from '../../i18n';

export const setMembersSchema = z.object({
  memberIds: z
    .array(objectIdSchema)
    .min(1, { error: t(K.UNITS.MEMBERS_REQUIRED) }),
});

export type SetMembersDto = z.infer<typeof setMembersSchema>;
```

`dto/declare-leadership.dto.ts`: copia `src/unidades/dto/declarar-jefatura.dto.ts` renombrando `declararJefaturaSchema` a `declareLeadershipSchema` y `DeclararJefaturaDto` a `DeclareLeadershipDto`. El campo del cuerpo sigue llamándose `nombreCargo`: es dato del contrato con SiScout, no identificador.

- [ ] **Step 3: Escribir el servicio, parte 1: siembra y consulta**

```ts
import { Injectable } from '@nestjs/common';
import { InjectConnection, InjectModel } from '@nestjs/mongoose';
import { ClientSession, Connection, Model, Types } from 'mongoose';
import type { AuthUser } from '../auth/strategies/jwt.strategy';
import {
  cargosDeJefaturaDeRama,
  ramaDeCargo,
} from '../catalogo-cargos/catalogo-cargos';
import { AppBadRequestException, AppNotFoundException } from '../common';
import { CurrentUserService } from '../current-user/current-user.service';
import { D } from '../domain';
import { K } from '../i18n';
import { User, UserDocument } from '../users/schemas/user.schema';
import { projectMemberships } from './membership-projection';
import {
  UnitMembership,
  UnitMembershipDocument,
} from './schemas/unit-membership.schema';
import { Unit, UnitDocument } from './schemas/unit.schema';
import {
  placeholderName,
  planGroupSeed,
  type PlannedUnit,
} from './seeding/unit-seeder';
import { resolveUnitScope } from './unit-scope';

const DUPLICATE_KEY = 11000;

function isDuplicateKey(error: unknown): boolean {
  return (
    typeof error === 'object' &&
    error !== null &&
    'code' in error &&
    error.code === DUPLICATE_KEY
  );
}

@Injectable()
export class UnitsService {
  constructor(
    @InjectModel(Unit.name)
    private readonly unitModel: Model<UnitDocument>,
    @InjectModel(UnitMembership.name)
    private readonly membershipModel: Model<UnitMembershipDocument>,
    @InjectModel(User.name)
    private readonly userModel: Model<UserDocument>,
    @InjectConnection()
    private readonly connection: Connection,
    private readonly currentUser: CurrentUserService,
  ) {}

  async findAll(user: AuthUser): Promise<UnitDocument[]> {
    const profile = await this.currentUser.get(user.idSiscout!);
    const scope = resolveUnitScope(profile);

    switch (scope.type) {
      case 'all':
        return this.unitModel.find().exec();

      case 'group':
        return this.ofGroup(scope.groupId);

      case 'branch': {
        const units = await this.ofGroup(scope.groupId);
        return units.filter((unit) => unit.branch === scope.branch);
      }

      case 'no-group':
        throw new AppBadRequestException(K.UNITS.MISSING_GROUP);

      case 'leadership-required':
        throw new AppBadRequestException(
          K.UNITS.LEADERSHIP_REQUIRED,
          undefined,
          { jefaturas: cargosDeJefaturaDeRama() },
        );
    }
  }

  async findOne(id: string): Promise<UnitDocument> {
    const unit = await this.unitModel.findById(id).exec();
    if (!unit) throw new AppNotFoundException(K.UNITS.NOT_FOUND, { id });
    return unit;
  }

  private async ofGroup(groupId: number): Promise<UnitDocument[]> {
    const existing = await this.unitModel.find({ groupId }).exec();
    if (existing.length > 0) return existing;

    await this.seedGroup(groupId);
    return this.unitModel.find({ groupId }).exec();
  }

  async seedGroup(groupId: number): Promise<PlannedUnit[]> {
    const people = await this.userModel
      .find({ groupId, estado: true })
      .select('_id name tipo cargoSiscout cargos districtId districtName')
      .lean()
      .exec();

    const plan = planGroupSeed({
      groupId,
      people: people.map((p) => ({ ...p, _id: p._id.toString() })),
    });

    if (plan.units.length === 0) return [];

    await this.inTransaction(async (session) => {
      for (const planned of plan.units) {
        const [created] = await this.unitModel.create(
          [
            {
              ...planned,
              unitLeaderId: new Types.ObjectId(planned.unitLeaderId),
              members: planned.members.map((id) => new Types.ObjectId(id)),
            },
          ],
          { session },
        );
        await this.syncMembership(created, session);
      }
    });

    return plan.units;
  }

  private async inTransaction<T>(
    work: (session: ClientSession) => Promise<T>,
  ): Promise<T> {
    const session = await this.connection.startSession();
    try {
      return await session.withTransaction(() => work(session));
    } finally {
      await session.endSession();
    }
  }
}
```

- [ ] **Step 4: Escribir el servicio, parte 2: escrituras**

Añade estos métodos a la clase. `syncMembership` es el único punto donde se escriben `users.unitId` y `unit_memberships`, para que las tres colecciones no puedan divergir.

```ts
  private async syncMembership(
    unit: UnitDocument,
    session: ClientSession,
  ): Promise<void> {
    const unitId = unit._id.toString();

    const rows = projectMemberships({
      _id: unitId,
      groupId: unit.groupId,
      unitLeaderId: unit.unitLeaderId.toString(),
      leaders: unit.leaders.map((id) => id.toString()),
      members: unit.members.map((id) => id.toString()),
    });

    await this.membershipModel.deleteMany({ unitId: unit._id }, { session });
    await this.membershipModel.insertMany(
      rows.map((row) => ({
        userId: new Types.ObjectId(row.userId),
        unitId: new Types.ObjectId(row.unitId),
        role: row.role,
        groupId: row.groupId,
      })),
      { session },
    );

    await this.userModel.updateMany(
      { _id: { $in: unit.members } },
      { $set: { unitId: unit._id } },
      { session },
    );
  }

  async configure(id: string, dto: ConfigureUnitDto): Promise<UnitDocument> {
    return this.inTransaction(async (session) => {
      const unit = await this.unitModel.findById(id).session(session).exec();
      if (!unit) throw new AppNotFoundException(K.UNITS.NOT_FOUND, { id });

      unit.name = dto.name;
      unit.city = dto.city;
      unit.unitLeaderId = new Types.ObjectId(dto.unitLeaderId);
      unit.leaders = dto.leaders
        .filter((leaderId) => leaderId !== dto.unitLeaderId)
        .map((leaderId) => new Types.ObjectId(leaderId));
      unit.configuredAt = new Date();

      try {
        await unit.save({ session });
      } catch (error) {
        if (isDuplicateKey(error)) {
          throw new AppBadRequestException(K.UNITS.NAME_TAKEN);
        }
        throw error;
      }

      await this.syncMembership(unit, session);
      return unit;
    });
  }

  async setMembers(id: string, memberIds: string[]): Promise<UnitDocument[]> {
    return this.inTransaction(async (session) => {
      const unit = await this.unitModel.findById(id).session(session).exec();
      if (!unit) throw new AppNotFoundException(K.UNITS.NOT_FOUND, { id });

      const current = unit.members.map((m) => m.toString());
      const staying = new Set(memberIds);

      if (staying.size === 0) {
        throw new AppBadRequestException(K.UNITS.MEMBERS_REQUIRED);
      }
      if (memberIds.some((memberId) => !current.includes(memberId))) {
        throw new AppBadRequestException(K.UNITS.MEMBERS_NOT_IN_UNIT);
      }

      const leaving = current.filter((memberId) => !staying.has(memberId));

      unit.members = memberIds.map((memberId) => new Types.ObjectId(memberId));
      await unit.save({ session });
      await this.syncMembership(unit, session);

      if (leaving.length === 0) return [unit];

      const [clone] = await this.unitModel.create(
        [
          {
            name: await this.freeName(unit.groupId, unit.branch, session),
            branch: unit.branch,
            groupId: unit.groupId,
            districtId: unit.districtId,
            districtName: unit.districtName,
            city: unit.city,
            unitLeaderId: unit.unitLeaderId,
            leaders: [],
            members: leaving.map((memberId) => new Types.ObjectId(memberId)),
          },
        ],
        { session },
      );
      await this.syncMembership(clone, session);

      return [unit, clone];
    });
  }

  private async freeName(
    groupId: number,
    branch: Branch,
    session: ClientSession,
  ): Promise<string> {
    for (let index = 2; ; index += 1) {
      const candidate = placeholderName(branch, index);
      const taken = await this.unitModel
        .exists({ groupId, name: candidate })
        .session(session);
      if (!taken) return candidate;
    }
  }

  async update(id: string, dto: UpdateUnitDto): Promise<UnitDocument> {
    return this.inTransaction(async (session) => {
      const unit = await this.unitModel
        .findByIdAndUpdate(id, dto, {
          returnDocument: 'after',
          runValidators: true,
          session,
        })
        .exec();
      if (!unit) throw new AppNotFoundException(K.UNITS.NOT_FOUND, { id });

      await this.syncMembership(unit, session);
      return unit;
    });
  }

  async remove(id: string): Promise<void> {
    await this.inTransaction(async (session) => {
      const deleted = await this.unitModel
        .findByIdAndDelete(id, { session })
        .exec();
      if (!deleted) throw new AppNotFoundException(K.UNITS.NOT_FOUND, { id });

      await this.membershipModel.deleteMany(
        { unitId: deleted._id },
        { session },
      );
      await this.userModel.updateMany(
        { unitId: deleted._id },
        { $unset: { unitId: '' } },
        { session },
      );
    });
  }
```

Añade también `declareLeadership`, portando el método homónimo de `src/unidades/unidades.service.ts` con los nombres nuevos: `ramaDeCargo` sigue igual (es del catálogo de SiScout), `perfil` pasa a `profile`, y al final llama a `this.ofGroup(profile.groupId)` filtrando por la rama declarada.

Los imports de `Branch`, `ConfigureUnitDto` y `UpdateUnitDto` van arriba con el resto.

- [ ] **Step 5: Escribir el controlador**

Porta `src/unidades/unidades.controller.ts` con `@Controller('units')` y esta tabla:

| Método | Ruta | Decorador de permisos | Devuelve |
|---|---|---|---|
| `GET` | `/` | `@RequirePermissions('unit:read')` | `UnitDocument[]` |
| `GET` | `/:id` | `@RequirePermissions('unit:read')` | `UnitDocument` |
| `POST` | `/leadership` | `@RequirePermissions('unit:read')` | `UnitDocument[]` |
| `PATCH` | `/:id/configure` | `@RequirePermissions('unit:update')` | `UnitDocument` |
| `PATCH` | `/:id/members` | `@RequirePermissions('unit:update', 'unit:create')` | `UnitDocument[]` |
| `PATCH` | `/:id` | `@RequirePermissions('unit:update')` | `UnitDocument` |
| `DELETE` | `/:id` | `@RequirePermissions('unit:delete')` | `void`, con `@HttpCode(HttpStatus.NO_CONTENT)` |

**No** hay `POST /`. Los `@Param('id', ParseObjectIdPipe)` y los `@Body(new ZodValidationPipe(...))` se conservan tal como están en el controlador actual.

- [ ] **Step 6: Escribir el módulo y cablearlo**

```ts
import { Module } from '@nestjs/common';
import { MongooseModule } from '@nestjs/mongoose';
import { User, UserSchema } from '../users/schemas/user.schema';
import {
  UnitMembership,
  UnitMembershipSchema,
} from './schemas/unit-membership.schema';
import { Unit, UnitSchema } from './schemas/unit.schema';
import { UnitsController } from './units.controller';
import { UnitsService } from './units.service';

@Module({
  imports: [
    MongooseModule.forFeature([
      { name: Unit.name, schema: UnitSchema },
      { name: UnitMembership.name, schema: UnitMembershipSchema },
      { name: User.name, schema: UserSchema },
    ]),
  ],
  controllers: [UnitsController],
  providers: [UnitsService],
  exports: [UnitsService, MongooseModule],
})
export class UnitsModule {}
```

En `src/app.module.ts`, sustituye `UnidadesModule` por `UnitsModule`.

- [ ] **Step 7: Borrar el módulo viejo**

```bash
rm -rf be_ruta/src/unidades
```

- [ ] **Step 8: Actualizar el resto de referencias del backend**

```bash
cd be_ruta && rg -n "unidad|Unidad|UNIDAD" src/ test/ --glob '!src/i18n/**' -i
```

Revisa cada hit **uno a uno**. Recuerda: `comunidad` contiene `unidad` y NO se toca. Los mensajes del catálogo i18n siguen en español.

- [ ] **Step 9: Verificar el repo completo**

Run: `cd be_ruta && pnpm verify`
Expected: typecheck, lint, tests, i18n:check y domain:check en verde.

- [ ] **Step 10: Commit (solo con autorización del usuario)**

```bash
git add src/ && git commit -m "feat(units): reescribir el modulo de unidades en ingles con membresias"
```

---

### Task 9: CLI de siembra y reconstructor de membresías

**Files:**
- Create: `be_ruta/src/tools/seed-units.ts`
- Create: `be_ruta/src/tools/rebuild-unit-memberships.ts`
- Modify: `be_ruta/package.json` (scripts)

**Interfaces:**
- Consumes: `UnitsService.seedGroup` (Task 8), `projectMemberships` (Task 4).

- [ ] **Step 1: Escribir el CLI de siembra**

`src/tools/seed-units.ts` arranca un contexto Nest standalone (`NestFactory.createApplicationContext(AppModule)`), obtiene `UnitsService`, saca los `groupId` distintos con `userModel.distinct('groupId', { estado: true, groupId: { $ne: null } })`, y por cada uno llama a `seedGroup`. Al final imprime un resumen con: grupos sembrados, unidades creadas, y la lista de grupos saltados con su razón (`no-people`, `no-adults`, `no-protagonists`).

Es idempotente: un grupo que ya tiene unidades no se vuelve a sembrar.

- [ ] **Step 2: Escribir el reconstructor**

`src/tools/rebuild-unit-memberships.ts` recorre todas las unidades, borra las filas de `unit_memberships` de cada una e inserta las de `projectMemberships`. Imprime cuántas filas dejó. Es idempotente: correrlo dos veces produce el mismo resultado.

- [ ] **Step 3: Registrar los scripts**

En `be_ruta/package.json`, añade dentro de `scripts`:

```json
    "seed:units": "nest build && node dist/tools/seed-units.js",
    "units:rebuild-memberships": "nest build && node dist/tools/rebuild-unit-memberships.js",
```

- [ ] **Step 4: Probar contra la base local**

```bash
cd be_ruta && pnpm seed:units
```

Expected: resumen impreso sin excepciones. Corre de nuevo y verifica que la segunda pasada no crea unidades nuevas.

```bash
cd be_ruta && pnpm units:rebuild-memberships
```

Expected: el conteo de filas coincide con la suma de `1 + leaders + members` de cada unidad.

- [ ] **Step 5: Commit (solo con autorización del usuario)**

```bash
git add src/tools package.json
git commit -m "feat(units): agregar CLI de siembra y reconstructor de membresias"
```

---

## Fase 3: reset de datos

### Task 10: Borrar la base y volver a sembrar

**No hay migración de datos.** Los datos provienen de SiScout y se regeneran con el sync, así que la base se borra y se siembra de cero: todo nace ya con los nombres nuevos, los permisos `unit:*` y los índices correctos. Esto ahorra el script de migración entero.

**Files:**
- Modify: `be_ruta/README.md`

- [ ] **Step 1: Borrar la base**

```bash
cd be_ruta && mongosh "$MONGODB_URI" --quiet --eval "db.dropDatabase()"
```

Expected: `{ ok: 1 }`.

- [ ] **Step 2: Sembrar en orden**

```bash
cd be_ruta
pnpm seed:super-admin
pnpm seed:siscout-import
pnpm seed:units
```

El orden importa: `seed:units` necesita usuarios con `groupId`, que los pone `seed:siscout-import`.

- [ ] **Step 3: Verificar que la forma nueva quedó bien**

```bash
mongosh "$MONGODB_URI" --quiet --eval "
  print('unidades (coleccion vieja): ' + db.getCollectionNames().includes('unidades'));
  print('units: ' + db.units.countDocuments({}));
  print('unit_memberships: ' + db.unit_memberships.countDocuments({}));
  print('users con unitId: ' + db.users.countDocuments({unitId: {\$exists: true}}));
  print('roles con permiso viejo: ' + db.roles.countDocuments({permissions: /^unidad:/}));
  printjson(db.units.getIndexes().map(i => i.name));
"
```

Expected: `unidades` en `false`, `roles con permiso viejo` en `0`, conteos de `units` y `unit_memberships` mayores que cero, y los índices incluyen `groupId_1_name_1` y **no** `groupId_1_rama_1`.

- [ ] **Step 4: Verificar que las tres colecciones están cuadradas**

```bash
cd be_ruta && pnpm units:rebuild-memberships
```

Expected: el conteo de filas no cambia respecto del Step 3. Si cambia, `syncMembership` tiene un fallo: arréglalo antes de seguir.

- [ ] **Step 5: Documentar el procedimiento**

En `be_ruta/README.md`, añade una sección "Reiniciar la base en desarrollo" con los comandos de los Steps 1 y 2 y una advertencia de que borra todo y solo se usa en local.

- [ ] **Step 6: Commit (solo con autorización del usuario)**

```bash
git add README.md
git commit -m "docs(be): documentar el reinicio y resiembra de la base"
```

---

## Fase 4: PowerSync

### Task 11: Reglas de sync y espejo local

**Files:**
- Modify: `powersync/sync-config.yaml`
- Modify: `fe_ruta/lib/powersync/schema.ts`
- Modify: `powersync/README.md`

- [ ] **Step 1: Reescribir las reglas de sync**

Sustituye el bloque `bucket_definitions` de `powersync/sync-config.yaml` por:

```yaml
bucket_definitions:
  global_reference:
    data:
      - SELECT _id as id, * FROM cargos

  adults_of_the_group:
    parameters:
      - SELECT groupId FROM users
        WHERE _id = request.user_id() AND groupId IS NOT NULL
    data:
      - SELECT _id as id, * FROM users
        WHERE groupId = bucket.groupId AND tipo = 'adulto'
      - SELECT _id as id, * FROM units
        WHERE groupId = bucket.groupId

  units_of_the_member:
    parameters:
      - SELECT unitId FROM unit_memberships
        WHERE userId = request.user_id()
    data:
      - SELECT _id as id, * FROM unit_memberships
        WHERE unitId = bucket.unitId
      - SELECT _id as id, * FROM users
        WHERE unitId = bucket.unitId
      - SELECT _id as id, * FROM asistencia
        WHERE unitId = bucket.unitId
      - SELECT _id as id, * FROM progresion
        WHERE unitId = bucket.unitId
      - SELECT _id as id, * FROM diagnostico
        WHERE unitId = bucket.unitId
```

Son **dos** buckets a propósito. `adults_of_the_group` baja los adultos y las unidades del grupo, sin ningún dato de menores, y es lo que permite pintar el modal de configuración. `units_of_the_member` baja los protagonistas, y solo a quien tiene membresía en su unidad.

Sin el primero, la unidad quedaría bloqueada: el seed asigna un solo jefe, y si esa persona nunca entra a la app nadie más tendría datos para configurarla.

Conserva el comentario de cabecera del archivo, actualizando la advertencia de andamiaje para que siga diciendo la verdad: `asistencia`, `progresion` y `diagnostico` todavía no existen.

- [ ] **Step 2: Actualizar el espejo local**

En `fe_ruta/lib/powersync/schema.ts`, renombra `idUnidad` a `unitId` en la tabla `asistencia` (campo e índice) y añade las tres tablas que ahora bajan:

```ts
const units = new Table(
  {
    name: column.text,
    branch: column.text,
    groupId: column.integer,
    city: column.text,
    unitLeaderId: column.text,
    configuredAt: column.text,
  },
  { indexes: { group: ["groupId"] } },
);

const unit_memberships = new Table(
  {
    userId: column.text,
    unitId: column.text,
    role: column.text,
    groupId: column.integer,
  },
  { indexes: { unit: ["unitId"], user: ["userId"] } },
);

const users = new Table(
  {
    name: column.text,
    tipo: column.text,
    groupId: column.integer,
    unitId: column.text,
  },
  { indexes: { unit: ["unitId"], group: ["groupId"] } },
);

export const AppSchema = new Schema({
  asistencia,
  units,
  unit_memberships,
  users,
});
```

Añade los tipos derivados junto a `AsistenciaRecord`:

```ts
export type UnitRecord = Database["units"];
export type UnitMembershipRecord = Database["unit_memberships"];
export type PersonRecord = Database["users"];
```

`users` declara solo las columnas que consume la app. PowerSync baja el documento completo, pero el espejo local no necesita replicar campos que nadie lee, y cuantos menos declare, menos superficie de datos de menores queda en el dispositivo.

- [ ] **Step 3: Buscar consumidores del campo viejo**

```bash
cd fe_ruta && rg -n "idUnidad" app/ components/ lib/
```

Actualiza cada hit a `unitId`.

- [ ] **Step 4: Verificar el frontend**

Run: `cd fe_ruta && pnpm exec tsc --noEmit && pnpm test`
Expected: ambos en verde.

- [ ] **Step 5: Documentar el cambio de bucket**

En `powersync/README.md`, sección "Lo que falta para continuar", añade una nota explicando que el bucket se acota por `unit_memberships` y que por eso un dirigente de varias unidades recibe un bucket por cada una.

- [ ] **Step 6: Commit (solo con autorización del usuario)**

```bash
git add lib/powersync/schema.ts
git commit -m "feat(powersync): acotar el bucket por membresias de unidad"
```

---

## Fase 5: frontend

### Task 12: Componente `CheckboxList` en el banco

**Files:**
- Create: `fe_ruta/components/collection/checkbox-list.tsx`
- Create: `fe_ruta/components/collection/index.ts`
- Create: `fe_ruta/components/playground/checkbox-list-demo.tsx`
- Modify: `fe_ruta/app/diseno/componentes/page.tsx`
- Modify: `fe_ruta/lib/i18n/catalogo.ts`

**Interfaces:**
- Produces: `CheckboxList`, con props `{ items: CheckboxListItem[]; selected: string[]; onChange: (ids: string[]) => void; label: string; searchPlaceholder: string; emptyMessage: string; disabled?: boolean }` y `CheckboxListItem = { id: string; label: string; hint?: string; disabled?: boolean }`.

- [ ] **Step 1: Añadir las claves i18n**

En `fe_ruta/lib/i18n/catalogo.ts`, dentro del bloque `UNITS`, añade en orden alfabético:

```ts
    ASSISTANTS: "Subjefes",
    CONFIGURE_DESCRIPTION:
      "Antes de empezar, dale un nombre a tu unidad y dinos quién la acompaña.",
    CONFIGURE_SUBMIT: "Guardar unidad",
    CONFIGURE_TITLE: "Configura tu unidad",
    MEMBERS: "Protagonistas",
    MEMBERS_REQUIRED: "Una unidad necesita al menos un protagonista.",
    NAME_TAKEN: "Ya existe una unidad con ese nombre en tu grupo.",
    SEARCH_PEOPLE: "Buscar por nombre...",
    SELECTED_COUNT: "{selected} de {total}",
    SPLIT_WARNING:
      "Saldrán {total} protagonistas y se creará una unidad nueva con ellos.",
    UNIT_LEADER: "Jefe de unidad",
```

Run: `cd fe_ruta && pnpm i18n:check`
Expected: PASS.

- [ ] **Step 2: Escribir el componente**

Compón sobre primitivas del banco: `Checkbox` de `@/components/ui/checkbox`, `Input` de `@/components/ui/input`, `ScrollArea` de `@/components/ui/scroll-area`, `Badge` de `@/components/ui/badge`, `Label` de `@/components/ui/label`.

Requisitos:
- Filtra `items` por el texto de búsqueda, sin distinguir mayúsculas ni tildes.
- Muestra `label` arriba y el contador `SELECTED_COUNT` a la derecha.
- Cada fila: `Checkbox` + etiqueta + `hint` opcional en `text-muted-foreground`.
- Una fila con `disabled: true` no se puede marcar.
- Si el filtro no deja nada, muestra `emptyMessage`.
- El área de lista scrollea con altura máxima, no crece sin límite.
- Foco visible por teclado. Recuerda el gotcha de Tailwind v4: usa `focus-visible:outline-solid`, porque `outline-none` mata el grosor heredado.

- [ ] **Step 3: Exportar desde el índice**

```ts
export { CheckboxList } from "./checkbox-list";
export type { CheckboxListItem } from "./checkbox-list";
```

- [ ] **Step 4: Escribir la demo**

`components/playground/checkbox-list-demo.tsx` sigue el patrón de las demos existentes (mira `checks-demo.tsx`): envuelve en `DemoCard`, usa estado local y datos de ejemplo, e incluye al menos una fila deshabilitada con `hint` para que se vea el caso.

- [ ] **Step 5: Registrar la demo en el catálogo**

En `app/diseno/componentes/page.tsx`, importa `CheckboxListDemo` y colócala en una de las tres columnas del grid, junto a las demás.

- [ ] **Step 6: Verificar visualmente**

Levanta `pnpm dev` y abre `/diseno/componentes`. Comprueba: búsqueda que filtra, contador que actualiza, fila deshabilitada que no se marca, y navegación con Tab que muestra el foco.

Usa la skill `verificar-visual` del repo. **No uses el preview MCP**: no funciona con Next 16 en este proyecto.

- [ ] **Step 7: Verificar el repo**

Run: `cd fe_ruta && pnpm exec tsc --noEmit && pnpm lint && pnpm i18n:check`
Expected: en verde.

- [ ] **Step 8: Commit (solo con autorización del usuario)**

```bash
git add components/collection components/playground app/diseno lib/i18n
git commit -m "feat(ui): agregar CheckboxList al banco de componentes"
```

---

### Task 13: Modelo tipado y frontera del cliente

**Files:**
- Modify: `fe_ruta/lib/units/types.ts`
- Test: `fe_ruta/lib/units/types.test.ts`
- Modify: `fe_ruta/lib/backend/client.ts:375-384`
- Modify: `fe_ruta/lib/domain/endpoints.ts:34-35`

**Interfaces:**
- Produces: `Unit` con `{ _id, name, branch, groupId, districtId?, districtName?, city?, unitLeaderId, leaders, members, configuredAt? }`; `isConfigured(unit): boolean`; métodos de `backend`: `listUnits`, `getUnit`, `configureUnit`, `setUnitMembers`, `declareLeadership`.
- `getUnit` devuelve solo el documento de la unidad. Las listas de personas **no viajan por HTTP**: salen del SQLite local de PowerSync en la Task 14.

- [ ] **Step 1: Actualizar los endpoints**

En `lib/domain/endpoints.ts`, sustituye el bloque `UNIDADES` por:

```ts
  UNITS: {
    LIST: "/units",
    LEADERSHIP: "/units/leadership",
    detail: (id: string) => `/units/${id}`,
    configure: (id: string) => `/units/${id}/configure`,
    members: (id: string) => `/units/${id}/members`,
  },
```

- [ ] **Step 2: Escribir el test que falla**

Añade a `lib/units/types.test.ts`:

```ts
import { isConfigured } from "./types";

describe("isConfigured", () => {
  it("una unidad recien sembrada no esta configurada", () => {
    expect(isConfigured({ configuredAt: undefined })).toBe(false);
  });

  it("una unidad con fecha de configuracion si lo esta", () => {
    expect(isConfigured({ configuredAt: "2026-07-27T10:00:00.000Z" })).toBe(true);
  });

  it("una cadena vacia no cuenta como configurada", () => {
    expect(isConfigured({ configuredAt: "" })).toBe(false);
  });
});
```

- [ ] **Step 3: Ejecutar el test para verificar que falla**

Run: `cd fe_ruta && pnpm exec vitest run lib/units/types.test.ts`
Expected: FAIL, `isConfigured` no exportado.

- [ ] **Step 4: Actualizar el modelo**

En `lib/units/types.ts`, sustituye la interfaz `Unit` y añade el helper:

```ts
export interface Unit {
  _id: string;
  name: string;
  branch: Branch;
  groupId: number;
  districtId?: number;
  districtName?: string;
  city?: string;
  unitLeaderId: string;
  leaders: string[];
  members: string[];
  configuredAt?: string;
}

export function isConfigured(unit: Pick<Unit, "configuredAt">): boolean {
  return Boolean(unit.configuredAt);
}
```

Actualiza también los tests existentes del archivo que usaran `nombre` o `rama`.

- [ ] **Step 5: Ejecutar los tests para verificar que pasan**

Run: `cd fe_ruta && pnpm exec vitest run lib/units/types.test.ts`
Expected: PASS.

- [ ] **Step 6: Actualizar la frontera del cliente**

En `lib/backend/client.ts`, sustituye los dos métodos de unidades por:

```ts
  listUnits(token: string): Promise<Unit[]> {
    return request<Unit[]>(ENDPOINTS.UNITS.LIST, { token });
  },
  getUnit(token: string, id: string): Promise<Unit> {
    return request<Unit>(ENDPOINTS.UNITS.detail(id), { token });
  },
  configureUnit(
    token: string,
    id: string,
    body: {
      name: string;
      unitLeaderId: string;
      leaders: string[];
      city: string;
    },
  ): Promise<Unit> {
    return request<Unit>(ENDPOINTS.UNITS.configure(id), {
      method: "PATCH",
      body,
      token,
    });
  },
  setUnitMembers(
    token: string,
    id: string,
    memberIds: string[],
  ): Promise<Unit[]> {
    return request<Unit[]>(ENDPOINTS.UNITS.members(id), {
      method: "PATCH",
      body: { memberIds },
      token,
    });
  },
  declareLeadership(token: string, nombreCargo: string): Promise<Unit[]> {
    return request<Unit[]>(ENDPOINTS.UNITS.LEADERSHIP, {
      method: "POST",
      body: { nombreCargo },
      token,
    });
  },
```

- [ ] **Step 7: Verificar el repo**

Run: `cd fe_ruta && pnpm exec tsc --noEmit && pnpm test`
Expected: en verde. Los errores de tipo que aparezcan en `app/(privado)/unidades/` los resuelve la Task 14.

- [ ] **Step 8: Commit (solo con autorización del usuario)**

```bash
git add lib/units lib/backend lib/domain
git commit -m "feat(units): traducir el modelo tipado y la frontera del cliente"
```

---

### Task 14: Pantallas de unidades

**Files:**
- Create: `fe_ruta/app/(privado)/units/page.tsx`, `[id]/page.tsx`, `[id]/unit-detail-client.tsx`, `[id]/configure-dialog.tsx`, `[id]/members-form.tsx`, `[id]/unit-detail-skeleton.tsx`, `actions.ts`, `leadership-picker.tsx`
- Delete: `fe_ruta/app/(privado)/unidades/`
- Modify: `fe_ruta/components/app/nav.ts`, `fe_ruta/AGENTS.md`
- Modify: `fe_ruta/lib/domain/` (constante `ROUTES`)

**Interfaces:**
- Consumes: `CheckboxList` (Task 12), `Unit`, `isConfigured`, métodos de `backend` (Task 13), `PersonRecord` y `PowerSyncProvider` (Task 11).

- [ ] **Step 1: Mover la carpeta y traducir el listado**

```bash
cd fe_ruta && git mv "app/(privado)/unidades" "app/(privado)/units"
```

En `page.tsx`, actualiza `UnitCard` para usar `unit.name` y `unit.branch`, y `requirePermission("unit:read")`. Envuelve cada tarjeta en un enlace a `/units/${unit._id}`.

- [ ] **Step 2: Actualizar la ruta en navegación y dominio**

```bash
cd fe_ruta && rg -n "/unidades" app/ components/ lib/
```

Actualiza cada hit a `/units`, incluida la entrada de `ROUTES` y el ítem del menú en `components/app/nav.ts`. El **texto** del menú sigue siendo "Unidades" (clave i18n), solo cambia la URL.

- [ ] **Step 3: Escribir el detalle**

`app/(privado)/units/[id]/page.tsx` es un server component delgado: llama a `requirePermission("unit:read")`, obtiene la unidad con `backend.getUnit`, y delega en un client component pasándole la unidad. **No resuelve personas**: eso lo hace PowerSync en el cliente.

```tsx
export default async function UnitPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  await requirePermission("unit:read");

  const { id } = await params;
  const { accessToken } = await readTokens();
  if (!accessToken) return null;

  const unit = await backend.getUnit(accessToken, id);

  return <UnitDetailClient unit={unit} />;
}
```

Recuerda que en Next 16 `params` es una promesa.

- [ ] **Step 4: Escribir el cliente del detalle**

`unit-detail-client.tsx` va envuelto en `PowerSyncProvider` y lee las dos listas del SQLite local, con el mismo patrón de `components/campo/asistencia-cliente.tsx`:

```tsx
"use client";

import { useQuery } from "@powersync/react";
import { PowerSyncProvider } from "@/lib/powersync/provider";
import type { PersonRecord } from "@/lib/powersync/schema";
import { isConfigured, type Unit } from "@/lib/units/types";

function Detail({ unit }: { unit: Unit }) {
  const { data: members, isLoading: loadingMembers } = useQuery<PersonRecord>(
    "SELECT * FROM users WHERE unitId = ? AND tipo = 'protagonista' ORDER BY name",
    [unit._id],
  );

  const { data: adults, isLoading: loadingAdults } = useQuery<PersonRecord>(
    "SELECT * FROM users WHERE groupId = ? AND tipo = 'adulto' ORDER BY name",
    [unit.groupId],
  );

  if (loadingMembers || loadingAdults) return <UnitDetailSkeleton />;

  if (!isConfigured(unit)) {
    return <ConfigureDialog unit={unit} adults={adults} />;
  }

  return <MembersForm unit={unit} members={members} />;
}

export function UnitDetailClient({ unit }: { unit: Unit }) {
  return (
    <PowerSyncProvider>
      <Detail unit={unit} />
    </PowerSyncProvider>
  );
}
```

`UnitDetailSkeleton` se compone con `Skeleton` de `@/components/ui/skeleton`. **Es obligatorio**: la primera vez que alguien entra, el SQLite local está vacío hasta que baja el bucket. Sin el estado de carga vería una unidad sin protagonistas y creería que no tiene ninguno.

Monta también `SyncStatusIndicator` de `components/sync/sync-status-indicator.tsx`, que ya existe, para que se vea si el dispositivo está sincronizando.

- [ ] **Step 5: Escribir el modal de configuración**

`configure-dialog.tsx` es un client component sobre `Dialog` de `@/components/ui/dialog`, abierto de entrada y sin cierre por overlay ni por Escape. Recibe `adults: PersonRecord[]`. Contiene:
- `Input` para `name` (`K.UNITS.UNIT_NAME`),
- `Select` o `Combobox` del banco para `unitLeaderId` (`K.UNITS.UNIT_LEADER`), poblado con `adults`,
- `CheckboxList` para `leaders` (`K.UNITS.ASSISTANTS`), poblado con `adults` menos el jefe seleccionado,
- `Input` para `city` (`K.UNITS.CITY`),
- botón de envío con `K.UNITS.CONFIGURE_SUBMIT`.

Deshabilita el envío si `name` está vacío o no hay jefe seleccionado. Si `adults` llega vacío, muestra `Empty` con un aviso de que aún no hay adultos sincronizados en el grupo, en vez de un formulario inservible.

- [ ] **Step 6: Escribir el formulario de miembros**

`members-form.tsx` recibe `members: PersonRecord[]` y pinta un `CheckboxList` con todos marcados de entrada. Cuando el número de marcados es menor que el total, muestra un aviso con `K.UNITS.SPLIT_WARNING` interpolando cuántos salen. Deshabilita el envío si no queda ninguno marcado.

**La escritura NO va por la cola de PowerSync**: llama a la server action `setUnitMembers`, que habla con el backend. Guardar crea unidades y escribe en tres colecciones dentro de una transacción, y eso no es una escritura encolable. Si no hay señal, la lista se lee pero no se puede reorganizar: deshabilita el botón cuando el conector esté desconectado.

- [ ] **Step 7: Escribir las server actions**

En `actions.ts`, junto a `declareLeadership`, añade `configureUnit` y `setUnitMembers`. Ambas siguen el patrón existente: `requireAccess()`, `readTokens()`, llamada al `backend`, captura de `BackendError` devolviendo `{ error: error.message }`, `revalidatePath` y retorno.

- [ ] **Step 8: Actualizar AGENTS.md**

En `fe_ruta/AGENTS.md`, la decisión de PowerSync dice hoy que "solo las superficies de campo (`/campo/*`) funcionan sin señal" y que "el panel de administración sigue siendo SSR online-only". Ya no es cierto del todo. Actualízala para reflejar que `/units/[id]` lee sus listas de personas del SQLite local (dos buckets: `adults_of_the_group` y `units_of_the_member`) pero escribe siempre contra el backend, y que el listado `/units` sigue siendo SSR porque solo el servidor resuelve el alcance de un super admin o de un nivel región.

- [ ] **Step 9: Verificar el repo**

Run: `cd fe_ruta && pnpm exec tsc --noEmit && pnpm lint && pnpm test && pnpm i18n:check && pnpm domain:check`
Expected: todo en verde.

- [ ] **Step 10: Verificar visualmente el flujo completo**

Con `be_ruta` corriendo y datos sembrados:
1. Entra a `/units` y comprueba que lista las unidades.
2. Abre una sin configurar y comprueba que el modal aparece y no se puede descartar.
3. Guarda nombre, jefe, subjefes y ciudad. Comprueba que el modal se cierra y aparece el detalle.
4. Desmarca dos protagonistas, comprueba el aviso de separación y guarda.
5. Vuelve a `/units` y comprueba que hay una unidad nueva con los dos que salieron.
6. Abre la nueva y comprueba que le sale el modal de configuración.

Añade dos comprobaciones propias de PowerSync:

7. Entra al detalle con la caché local vacía (borra el IndexedDB del navegador) y comprueba que aparece el esqueleto de carga y no una lista vacía.
8. Desconecta la red, recarga el detalle y comprueba que la lista se sigue viendo pero el botón de guardar está deshabilitado.

Usa la skill `verificar-visual`.

- [ ] **Step 11: Commit (solo con autorización del usuario)**

```bash
git add app/ components/ lib/ AGENTS.md
git commit -m "feat(units): agregar configuracion inicial y separacion de unidades"
```

---

### Task 15: Barrido final del renombrado

**Files:**
- Modify: los que salgan del barrido, en los dos repos

- [ ] **Step 1: Barrer be_ruta**

```bash
cd be_ruta && rg -n "unidad|Unidad|UNIDAD" src/ test/ scripts/ -i
```

Cada hit debe ser una de estas tres cosas: prosa en español (mensajes i18n, documentación), la rama `comunidad`, o el nombre de una colección de campo aún no creada. Cualquier otra cosa se traduce.

- [ ] **Step 2: Barrer fe_ruta**

```bash
cd fe_ruta && rg -n "unidad|Unidad|UNIDAD" app/ components/ lib/ test/ -i
```

Mismo criterio.

- [ ] **Step 3: Verificar los dos repos**

```bash
cd be_ruta && pnpm verify
cd ../fe_ruta && pnpm exec tsc --noEmit && pnpm lint && pnpm test && pnpm i18n:check && pnpm domain:check
```

Expected: todo en verde.

- [ ] **Step 4: Confirmar que los manifiestos siguen idénticos**

```bash
sha256sum be_ruta/domain-manifest.json fe_ruta/domain-manifest.json
```

Expected: hashes idénticos.

- [ ] **Step 5: Actualizar ROADMAP.md**

En `fe_ruta/ROADMAP.md`, marca `[x]` lo que este trabajo cierra de la sección de unidades.

- [ ] **Step 6: Commit (solo con autorización del usuario)**

```bash
git commit -am "chore(units): completar el renombrado a ingles"
```

---

## Verificación final

- [ ] `pnpm seed:units` siembra, reporta grupos saltados y es idempotente.
- [ ] Entrar a `/units` con un grupo sin unidades las siembra en el momento.
- [ ] Una unidad sin `configuredAt` abre el modal y al guardar lo sella.
- [ ] Desmarcar miembros crea una unidad nueva con los salientes y nombre libre.
- [ ] Ninguna unidad queda con cero miembros, ni por UI ni por API.
- [ ] Dos unidades del mismo grupo no comparten nombre.
- [ ] `pnpm units:rebuild-memberships` tras cualquier operación no produce cambios.
- [ ] La parameter query de `units_of_the_member` devuelve una fila por unidad de la persona.
- [ ] El detalle pinta los protagonistas leyendo del SQLite local, sin pedirlos por HTTP.
- [ ] Con la caché local vacía, el detalle muestra el esqueleto de carga y nunca una lista vacía engañosa.
- [ ] Un dirigente del grupo sin membresía en la unidad puede abrir el modal, reasignar la jefatura y guardarse a sí mismo. Tras guardar, ya ve los protagonistas.
- [ ] Sin señal, el detalle se lee pero el botón de guardar queda deshabilitado.
- [ ] Borrar la base y correr los tres seeds deja el sistema operativo, sin restos de `unidades`, `idUnidad` ni permisos `unidad:*`.
- [ ] `pnpm verify` en be_ruta y la batería equivalente en fe_ruta, en verde.
- [ ] El barrido de `unidad` solo devuelve prosa en español, `comunidad` y colecciones de campo por crear.
